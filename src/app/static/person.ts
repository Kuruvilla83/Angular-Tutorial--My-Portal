//Below we declared an interface which is also call as a type for calling typescript classes.
export interface Person {
  id: number;
  name: string;
  address: string;
  sex: string;
  active: boolean;
}
